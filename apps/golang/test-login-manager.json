{
    "query": "mutation WebLogin($input: WebLoginInput!) { webLogin(input: $input) { success message user { id username nama role } } }",
    "variables": {
        "input": {
            "identifier": "manager",
            "password": "demo123"
        }
    }
}