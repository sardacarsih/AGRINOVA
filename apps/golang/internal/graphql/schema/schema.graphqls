# =============================================================================
# Root Schema - Combines all domain schemas
# =============================================================================

# Root types that are extended by domain schemas
type Query
type Mutation  
type Subscription