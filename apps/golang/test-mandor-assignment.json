{
    "query": "mutation MobileLogin($input: MobileLoginInput!) { mobileLogin(input: $input) { accessToken user { id username name role } assignments { divisions { id name code estateId blocks { id blockCode name } } estates { id name } } } }",
    "variables": {
        "input": {
            "identifier": "mandor",
            "password": "demo123",
            "platform": "ANDROID"
        }
    }
}