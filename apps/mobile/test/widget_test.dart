// Smoke test — memastikan project dapat dianalisa dan di-test oleh CI.
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('smoke test', () {
    expect(1 + 1, equals(2));
  });
}
